# Dynamically add ui elements for downloadButton

Dynamically add ui elements for downloadButton

## Usage

``` r
createDownloadModuleUI(output, private_key)
```

## Arguments

- output:

  shiny session output object

- private_key:

  Private key used for the conditional logic to add a signed JSON
  download button or not
