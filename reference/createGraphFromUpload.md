# Creates a graph from a (uploaded) list

Creates a graph from a (uploaded) list

## Usage

``` r
createGraphFromUpload(graph_list)
```

## Arguments

- graph_list:

  list, containing all elements required for the graph
